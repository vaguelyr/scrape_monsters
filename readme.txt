I found this neat website about monsters but the search function is limited.

So my idea is to use the html behind its random monster button to scrape most of the pages and create a sql database for all the monsters, and learn sql by searching that instead.
